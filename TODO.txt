10.7.21
סיכום גרסה 1.1, ודברים להמשך:
סיכום:
- סידור קוד
- שיפור GUI
- הוספת פ'יצרים:
  * חלון "מה חדש?"
 * הורדת כפתור מזעור חלון, והגדלה\הקטנה - אולי מתישהו להוסיף אפשרות למסך מלא, למרות שדי מיותר
 * לוודא אישור סגירת חלון - כשלוחצים על האיקס

לגרסה הבאה:
- נכון לעכשיו, אני רק שומר ערך אחר בקובץ log אם ההגדרות לא שונו - הערך החדש זהה לישן, אך לא מודיע למשתמש. להוסיף בקרה על זה.
 * בעיות: שינוי הפונקציות מ void ל int / bool - לבקרה. כנראה שאני אצטרך רק bool - ערך אחד.
   -*- טופל!


 -*- לבדוק אם בכלל צריך:

- כשאני ניגש להגדרות, אני משתמש בפונקציות (בעיקר ב handleSettings.cs) כמו:
 - getProperty
 - updateSettings
  ואני מעביר כפרמטר ערך לחיפוש הגדרות (לדוגמא "data", שזה היחיד שקיים עכשיו), אני לא בודק אם הערך הזה בכלל קיים - צריך להשתמש בפונקציות שהתחלתי לכתוב, כמו:
 - isExist_stringArray (Sys.cs)
- checkIfSettingsExists (handleSettings.cs)
 - string[] possibleSettings (Sys.cs)
 בינתיים הפונקציות והמערך קיימים, אך ללא כול שימוש.
